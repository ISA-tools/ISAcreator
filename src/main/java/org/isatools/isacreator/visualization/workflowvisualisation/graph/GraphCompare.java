package org.isatools.isacreator.visualization.workflowvisualisation.graph;


public class GraphCompare {

    /**
     * In this class, we wish to compare graphs, or experiments to
     * find similarities in processing workflows.
     *
     * If we validate against the configuration,
     * we wish to visually see which experiments are valid and
     * more importantly, which aren't.
     */
}
